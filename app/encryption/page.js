import EncryptionForm from '../../components/EncryptionForm';  // Fix the import path

export default function EncryptionPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-100">
      <EncryptionForm />
    </div>
  );
}